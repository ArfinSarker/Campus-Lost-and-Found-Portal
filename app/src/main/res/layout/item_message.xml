<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="6dp">

    <!-- Receiver Avatar -->
    <ImageView
        android:id="@+id/ivUserAvatar"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="4dp"
        android:layout_marginEnd="8dp"
        android:layout_marginBottom="4dp"
        android:src="@drawable/ic_user"
        android:background="@drawable/bg_circle_avatar"
        android:padding="4dp"
        android:visibility="gone" />

    <!-- Message bubble card -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardMessageBubble"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:cardCornerRadius="18dp"
        app:cardElevation="0dp"
        app:strokeWidth="0dp"
        android:layout_alignParentStart="true">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="16dp"
            android:paddingVertical="10dp"
            android:minWidth="80dp">

            <!-- Reply Preview in Bubble -->
            <LinearLayout
                android:id="@+id/layoutReplyBubble"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:paddingHorizontal="10dp"
                android:paddingVertical="6dp"
                android:layout_marginBottom="6dp"
                android:background="@drawable/bg_reply_bubble_receiver"
                android:clickable="true"
                android:focusable="true"
                android:visibility="gone"
                android:gravity="center_vertical">

                <View
                    android:id="@+id/replyLine"
                    android:layout_width="2dp"
                    android:layout_height="24dp"
                    android:background="@color/messenger_accent"
                    android:layout_marginEnd="6dp"/>

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/tvReplyBubbleName"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="John Doe"
                        android:textColor="@color/messenger_accent"
                        android:textSize="11sp"
                        android:textStyle="bold" />

                    <TextView
                        android:id="@+id/tvReplyBubbleText"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Replied text preview..."
                        android:textColor="@color/messenger_text_secondary"
                        android:textSize="11sp"
                        android:maxLines="1"
                        android:ellipsize="end" />
                </LinearLayout>
            </LinearLayout>

            <TextView
                android:id="@+id/tvMessageText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Message body content..."
                android:textColor="@color/textPrimary"
                android:textSize="15sp" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:layout_marginTop="2dp"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tvMessageTime"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="12:00 PM"
                    android:textColor="@color/textSecondary"
                    android:textSize="10sp" />

                <ImageView
                    android:id="@+id/ivMessageStatus"
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:layout_marginStart="4dp"
                    android:src="@drawable/ic_single_check"
                    android:visibility="gone" />
            </LinearLayout>
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Reactions Container -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardReactionsContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/cardMessageBubble"
        android:layout_marginTop="-6dp"
        app:cardCornerRadius="10dp"
        app:cardElevation="1dp"
        app:strokeWidth="1dp"
        app:strokeColor="@color/messenger_card_border"
        app:cardBackgroundColor="@color/messenger_card_bg"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingHorizontal="6dp"
            android:paddingVertical="2dp"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tvReactionsEmojis"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="❤️"
                android:textSize="11sp" />

            <TextView
                android:id="@+id/tvReactionsCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="1"
                android:textColor="@color/messenger_text_secondary"
                android:textSize="11sp"
                android:layout_marginStart="4dp" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

</RelativeLayout>
